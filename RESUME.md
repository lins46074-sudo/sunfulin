# 简历材料 · 本地知识库 RAG 问答智能体

> 本文件是**求职专用**的素材库，不是给别人看的项目文档。
> 里面的内容按「可直接复制粘贴」组织，按场景取用即可。
> 项目本身的说明见 [README.md](README.md)，技术设计见 [docs/DESIGN.md](docs/DESIGN.md)。

---

## 一、项目基本信息（填表用）

| 字段 | 内容 |
|---|---|
| 项目名称 | 本地知识库 RAG 问答智能体（LangChain RAG Agent） |
| 项目角色 | 独立开发（个人项目） |
| 项目周期 | 2026.09 |
| 代码规模 | 7 个 Python 模块 + 21 个单元测试，约 600 行 |
| 技术栈 | Python 3.10+ / LangChain 1.x / Chroma / BGE / Streamlit / uv |
| 开源地址 | https://github.com/lins46074-sudo/sunfulin |

---

## 二、一句话版本（简历标题行 / 自我介绍用）

> 基于 LangChain 1.x 的 Tool-Calling Agent 实现私有文档 RAG 问答系统，
> 支持 Agent 自主决策检索、来源可溯源与幻觉抑制，提供命令行与 Web 双端交互。

---

## 三、简历精简版（推荐，4 条 bullet）

直接复制到简历的「项目经历」下：

- **Agent 编排**：基于 LangChain 1.x `create_agent` 实现 Tool-Calling 智能体，
  自定义知识库检索工具，由模型自主判断是否需要检索；借助 Checkpointer 持久化会话状态，
  支持「那免费版呢？」这类省略式多轮追问。
- **检索链路**：Chroma 本地持久化 + BAAI/bge-small-zh-v1.5 中文向量模型，
  针对中文语义特点自定义标点优先分隔符序列，解决固定字数切片把句子拦腰截断、
  导致召回片段语义不完整的问题。
- **幻觉抑制**：设计「来源编号标注 + 检索结果结构化 + 拒答兜底话术」三层约束，
  检索工具直接返回带编号的格式化文本，使来源标注由数据链路保证而非依赖提示词约束；
  知识库无相关内容时明确拒答而非编造。
- **工程实践**：检索工具与 Agent 组装逻辑抽为公共模块，命令行与 Streamlit 双端零重复复用；
  配置全部外置为环境变量，切换模型端点无需改代码；配套 21 个单元测试与 GitHub Actions CI。

---

## 四、详细版（作品集 / 面试自我介绍，约 400 字）

面向私有文档（`.txt` / `.md`）的检索增强生成问答系统。用户用自然语言提问，
Agent 自主决策是否调用检索工具——常识问题直接回答，文档相关问题才触发检索；
若首轮召回不足以回答，模型会改写关键词重试。

实现上有三个着力点：

**一是 Agent 编排。** 基于 LangChain 1.x 的 `create_agent` 构建 Tool-Calling 智能体，
检索能力封装为标准工具，工具的 docstring 即模型可见的能力边界描述。
会话状态通过 Checkpointer 持久化，命令行端与 Web 端各自持有独立的线程 ID，
实现多轮上下文隔离。

**二是检索质量。** 中文场景下，按固定字数切片会把句子从中间截断，
召回的片段语义残缺，直接影响回答质量。项目自定义了
`["\n\n", "\n", "。", "！", "？", "；", "，", " ", ""]` 的优先分隔符序列，
优先在段落、句末等自然语义边界切分，把句子被截断的概率降到最低——
这一点有专门的回归测试守住，测试用例会用默认分隔符做对照组验证其有效性。

**三是幻觉抑制。** 检索工具不返回原始 Document 对象，而是返回
`[1] 来源：xxx` 这样带编号的结构化文本，模型在生成时天然知道每个片段出自哪份文档，
来源标注由数据链路保证而非依赖提示词约束；配合系统提示词中的固定拒答话术，
让模型在「没查到」时有明确的退路，而不是自行编造。

**工程方面**，检索工具与 Agent 组装抽为公共模块供双端复用，配置全部外置为环境变量
（切换模型端点只需改两行配置），并配套 21 个不依赖 API 凭证的单元测试与 CI 流水线。

---

## 五、面试高频追问与参考回答

### Q1：为什么不直接微调模型，而用 RAG？

微调和 RAG 解决的是不同问题。微调适合改变模型的**风格与能力**，
但用它注入事实知识有几个硬伤：成本高、知识更新需要重新训练、
且无法给出「这句话出自哪份文档」的溯源。私有文档场景下，
知识会频繁变动，同时业务上要求回答可核查，所以 RAG 是更合适的选择——
文档改了重跑一次建库脚本即可，秒级生效。

### Q2：为什么用 Agent，而不是固定的 RAG 链？

固定链是「每次提问都检索、检索完就把结果拼进提示词」。
但实际使用中大量提问是「你好」「你是谁」这类不需要查文档的，
无条件检索纯属浪费——既增加延迟，还可能把不相关的片段塞进上下文干扰模型。

用 Agent 把检索权交给模型自己判断，它看到问题后决定调不调工具。
虽然多了一次模型调用的开销，但换来了准确性，而且模型在召回不足时
还能改写关键词发起第二轮检索，这是固定链做不到的。

### Q3：中文切片为什么不能直接用默认参数？

`RecursiveCharacterTextSplitter` 的默认分隔符是 `["\n\n", "\n", " ", ""]`，
对中文来说只有换行和空格。中文文档常常整段没有换行，
一旦段落超过 `chunk_size`，就会被硬切——句子从中间断开，
半个句子进了这个片段、半个进了下一个片段，两个片段的语义都不完整，
向量化后检索质量直接下降。

我把中文句末标点加进分隔符序列并置于高优先级，
优先在段落、句号、问号等自然边界切分。这一点有回归测试守着：
测试构造 60 句结构规整的中文，逐块校验每个句子要么完整、
要么在句号处断开；同时用默认分隔符做对照组，确认断言能真正抓到截断问题。

### Q4：怎么防止模型幻觉？

三个层次的约束，可靠性递减但互相补位：

1. **数据链路层（最可靠）**：检索工具返回的是 `[1] 来源：price.md\n片段内容`
   这样的格式化文本，编号和来源是代码拼进去的，模型拿到手就知道每个片段的出处。
2. **提示词层**：系统提示词明确规定只依据检索内容作答、必须标注来源编号、
   知识库没有的内容必须回复固定话术 `【知识库没有找到相关内容】`。
3. **交互兜底**：Web 端每条回答下方可以展开查看本轮实际检索到的原文片段，
   用户能自己核对答案依据，这一点对内部工具的可信度很关键。

我在 README 里也写明了：提示词约束能显著降低幻觉，但并非 100% 可靠，
所以第一层数据链路的设计才是重点。

### Q5：多轮对话是怎么实现的？

LangChain 1.x 用 Checkpointer 做状态持久化。构建 Agent 时传入 `InMemorySaver`，
每次调用带上 `thread_id`，框架自动把该线程的历史消息存取并注入上下文。

命令行端固定用一个 `thread_id`；Web 端每个浏览器会话生成一个 UUID，
「清空对话」按钮的实现就是换一个新的 `thread_id`——
不需要手动清理任何消息列表，等于让 Agent 直接忘掉上下文。

### Q6：这个项目的检索效果怎么评估？

**说实话，目前没有做量化评估，这是这个项目最明显的短板。**
现阶段只能靠人工抽查——改完切片参数后，用一组固定问题手动对比召回片段是否相关。

如果要继续做，我会按这个顺序补：先整理一批「问题 → 应召回的文档片段」标注集，
再用 Hit Rate 和 MRR 这两个检索指标做定量评估，
最后引入 Ragas 之类框架评估生成质量（忠实度、答案相关性）。
有了基线数据，调 `TOP_K`、`chunk_size` 这些参数才有依据，
而不是像现在靠经验拍值。

### Q7：文档量涨到很大怎么办？（扩展性问题）

当前方案是本地 Chroma + 全量重建索引，适合个人/团队的小规模文档
（几十到几千份）。规模上量后有几个明确的瓶颈和对应做法：

- **建库**：现在每次重跑都全量删除重建，文档多了会很慢，
  应该改成增量索引——靠文件哈希判断哪些文档真的变了，只重建变动的部分。
- **检索**：向量检索在百万级以下够用，再往上需要引入 BM25 做混合检索，
  纯向量对专有名词、型号这类关键词的召回并不好。
- **服务**：现在是单机 Streamlit，多人在用时向量模型会在每个进程里各加载一份，
  应该把检索能力拆成独立服务，模型常驻、对外提供接口。

### Q8：为什么向量模型选 BAAI/bge-small-zh-v1.5？

三个考虑：**中文效果**（bge 系列在中文语义相似度任务上表现稳定，
small 版本在这个体量下够用）、**体积与速度**（95MB，CPU 推理即可，
不需要 GPU，降低了部署门槛）、**本地运行**（私有文档不出本机，
只把检索到的片段发给模型接口，这是数据合规上的硬要求）。

### Q9：这个项目你觉得最难/最有价值的部分是？

最难的是**把「不编造」这件事真正做扎实**。一开始我也只是在提示词里写
「不要编造」，但实测发现模型仍然会在召回内容不相关时，
拿常识硬凑一个看起来合理的答案。

真正的转折是意识到：不能指望提示词解决工程问题。
于是把来源信息做成结构化数据直接进上下文（而不是让模型自己去对应），
并给「查不到」设计一个明确的、模型愿意使用的出口话术。
这两点做完之后，拒答行为才稳定下来。

这件事让我理解到，做 LLM 应用时，**能用数据结构和代码保证的事情，
就不要交给提示词去求**。

### Q10：项目有什么已知局限？

我在 README 里也如实写了，主要有三点：

1. **没有量化评估**（见 Q6），参数调优靠经验，缺少数据支撑。
2. **全量重建索引**，文档变动后要重跑建库脚本，文档多时体验不好。
3. **会话状态存在内存**（`InMemorySaver`），进程重启后对话历史丢失，
   生产环境应换成 `SqliteSaver` 或 Redis 之类的持久化 Checkpointer。

---

## 六、技术关键词（供 HR 系统 / 岗位 JD 关键词匹配）

`Python` `LangChain` `RAG` `检索增强生成` `LLM 应用开发` `Agent`
`Tool Calling` `Function Calling` `Prompt Engineering` `提示词工程`
`Chroma` `向量数据库` `Vector Store` `Embedding` `文本向量化`
`BGE` `sentence-transformers` `语义检索` `文本切片` `Chunking`
`Streamlit` `Anthropic API` `Claude` `DeepSeek` `大模型接入`
`uv` `pytest` `单元测试` `GitHub Actions` `CI` `Git`

---

## 七、面试前自查清单

演示前确认这几件事，避免现场翻车：

- [ ] `.env` 里填了有效 key，`uv run python ingest.py` 跑通
- [ ] `uv run pytest -q` 全绿（21 项）
- [ ] `run_web.bat` 能起服务，浏览器正常打开
- [ ] 准备 3 个演示问题：一个文档内有答案的、一个需要多轮追问的、
      **一个知识库里查不到的**（用来展示拒答，这是这个项目最值得讲的行为）
- [ ] 本地能演示 `docs/` 放新文档 → 点「重建索引」→ 立刻能问到，展示知识更新的闭环
