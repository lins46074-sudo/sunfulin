# 项目笔记

> 开发过程中的设计问答、踩坑记录与后续计划，按时间累积。
> 项目说明见 [README.md](README.md)，技术设计见 [docs/DESIGN.md](docs/DESIGN.md)。

---

## 一、设计问答

用问答形式记录几个反复被问到、也确实值得想清楚的问题。

### Q1：为什么不直接微调模型，而用 RAG？

微调和 RAG 解决的是不同问题。微调适合改变模型的**风格与能力**，
但用它注入事实知识有几个硬伤：成本高、知识更新需要重新训练、
且无法给出「这句话出自哪份文档」的溯源。

私有文档场景下知识会频繁变动，同时业务上要求回答可核查，
所以 RAG 是更合适的选择——文档改了重跑一次建库脚本即可，秒级生效。

### Q2：为什么用 Agent，而不是固定的 RAG 链？

固定链是「每次提问都检索、检索完就把结果拼进提示词」。
但实际使用中大量提问是「你好」「你是谁」这类不需要查文档的，
无条件检索纯属浪费——既增加延迟，还可能把不相关的片段塞进上下文干扰模型。

用 Agent 把检索权交给模型自己判断，它看到问题后决定调不调工具。
虽然多了一次模型调用的开销，但换来了准确性，
而且模型在召回不足时还能改写关键词发起第二轮检索，这是固定链做不到的。

### Q3：中文切片为什么不能直接用默认参数？

`RecursiveCharacterTextSplitter` 的默认分隔符是 `["\n\n", "\n", " ", ""]`，
对中文来说只有换行和空格有效。中文文档常常整段没有换行，
一旦段落超过 `chunk_size`，就会被硬切——句子从中间断开，
半个句子进了这个片段、半个进了下一个片段，两个片段的语义都不完整，
向量化后检索质量直接下降。

把中文句末标点加进分隔符序列并置于高优先级后，切分点优先落在段落、句号、
问号等自然边界。这一点有回归测试守着：测试构造 60 句结构规整的中文，
逐块校验每个句子要么完整、要么在句号处断开；
同时用默认分隔符做对照组，确认断言能真正抓到截断问题。

### Q4：怎么防止模型幻觉？

三个层次的约束，可靠性递减但互相补位：

1. **数据链路层（最可靠）**：检索工具返回的是 `[1] 来源：price.md\n片段内容`
   这样的格式化文本，编号和来源是代码拼进去的，模型拿到手就知道每个片段的出处。
2. **提示词层**：系统提示词明确规定只依据检索内容作答、必须标注来源编号、
   知识库没有的内容必须回复固定话术 `【知识库没有找到相关内容】`。
3. **交互兜底**：Web 端每条回答下方可以展开查看本轮实际检索到的原文片段，
   用户能自己核对答案依据。

README 里也写明了：提示词约束能显著降低幻觉，但并非 100% 可靠，
所以第一层数据链路的设计才是重点。

### Q5：多轮对话是怎么实现的？

LangChain 1.x 用 Checkpointer 做状态持久化。构建 Agent 时传入 `InMemorySaver`，
每次调用带上 `thread_id`，框架自动把该线程的历史消息存取并注入上下文。

命令行端固定用一个 `thread_id`；Web 端每个浏览器会话生成一个 UUID，
「清空对话」按钮的实现就是换一个新的 `thread_id`——
不需要手动清理任何消息列表，等于让 Agent 直接忘掉上下文。

### Q6：检索效果怎么评估？

**目前没有做量化评估，这是项目当前最明显的一块空缺。**
现阶段只能靠人工抽查——改完切片参数后，用一组固定问题手动对比召回片段是否相关。

后续要补的话按这个顺序：先整理一批「问题 → 应召回的文档片段」标注集，
再用 Hit Rate 和 MRR 这两个检索指标做定量评估，
最后引入 Ragas 之类框架评估生成质量（忠实度、答案相关性）。
有了基线数据，调 `TOP_K`、`chunk_size` 这些参数才有依据，
而不是像现在靠经验拍值。

### Q7：文档量涨到很大怎么办？

当前方案是本地 Chroma + 全量重建索引，适合个人/团队的小规模文档
（几十到几千份）。规模上量后有几个明确的瓶颈和对应做法：

- **建库**：现在每次重跑都全量删除重建，文档多了会很慢，
  应该改成增量索引——靠文件哈希判断哪些文档真的变了，只重建变动的部分。
- **检索**：向量检索在百万级以下够用，再往上需要引入 BM25 做混合检索，
  纯向量对专有名词、型号这类关键词的召回并不好。
- **服务**：现在是单机 Streamlit，多人在用时向量模型会在每个进程里各加载一份，
  应该把检索能力拆成独立服务，模型常驻、对外提供接口。

### Q8：为什么向量模型选 BAAI/bge-small-zh-v1.5？

三个考虑：**中文效果**（bge 系列在中文语义相似度任务上表现稳定，
small 版本在这个体量下够用）、**体积与速度**（95MB，CPU 推理即可，
不需要 GPU，降低了部署门槛）、**本地运行**（私有文档不出本机，
只把检索到的片段发给模型接口，这是数据合规上的硬要求）。

### Q9：项目里最关键的设计是哪一个？

**检索工具返回格式化文本，而不是 Document 对象。**

一开始我只是在提示词里写「不要编造」，但实测发现模型在召回内容不相关时，
仍会拿常识硬凑一个看起来合理的答案。真正的转折是意识到：
不能指望提示词解决工程问题。

于是把来源信息做成结构化数据直接进上下文（而不是让模型自己去对应），
并给「查不到」设计一个明确的、模型愿意使用的出口话术。
这两点做完之后，拒答行为才稳定下来。

**能用数据结构和代码保证的事情，就不要交给提示词去求。**

### Q10：项目有什么已知局限？

按优先级：

1. **没有量化评估**（见 Q6），参数调优靠经验，缺少数据支撑。
2. **全量重建索引**，文档变动后要重跑建库脚本，文档多时体验不好。
3. **会话状态存在内存**（`InMemorySaver`），进程重启后对话历史丢失，
   生产环境应换成 `SqliteSaver` 或 Redis 之类的持久化 Checkpointer。
4. **纯向量检索**，专有名词、型号等关键词的召回偏弱，需要混合检索补足。

这些不是「没想到」，而是当前规模下不值得做。
个人与团队级文档用现在的方案完全够用，过早引入分布式检索反而是负担。

---

## 二、踩坑记录

开发过程中真实卡住过的地方，记下来省得重复踩。

### 1. `HF_ENDPOINT` 必须在导入 `langchain_huggingface` 之前设置

`huggingface_hub` 在**被导入时**就读取并缓存镜像地址，晚于它设置环境变量不会生效。
症状是模型下载**静默卡死**，没有任何报错，很容易误判成网络慢。

所以 `store.py` 的第一行是 `import config`，并附了注释说明原因。

### 2. Windows 控制台默认 GBK 编码

打印文档片段时，遇到 GBK 无法表示的字符会抛 `UnicodeEncodeError` 直接崩溃。
`config.py` 在导入时把标准输出强制切到 UTF-8。

顺带一提，用 `python -c` 或 `python -` 从标准输入执行脚本时**不会**走到这段逻辑，
仍然要靠 `PYTHONIOENCODING=utf-8` 环境变量。

### 3. `requires-python` 不能想当然写 `>=3.10`

`sentence-transformers` 依赖的 `onnxruntime` 只有 **cp311 及以上**的 wheel。
写成 `>=3.10` 时，Python 3.10 环境下 `uv sync` 会直接失败：

```
error: Distribution `onnxruntime==1.24.3` can't be installed because it
doesn't have a source distribution or wheel for the current platform
```

已改为 `>=3.11`，CI 的 matrix 也跟着改成 3.11 / 3.13 双版本。

### 4. 国内直连 GitHub 不稳定

`git fetch` / `git push` 直连 github.com 会间歇性报
`Recv failure: Connection was reset` 或
`Failed to connect to github.com:443 after 21000 ms`。
本机 `127.0.0.1:7890` 有可用代理，操作时带上环境变量即可：

```bash
https_proxy=http://127.0.0.1:7890 git push github main
```

不要为此改全局 git 配置——本项目的 `origin` 指向 Gitee，加全局代理会影响它。

### 5. 测试断言要用对照组验证，否则可能是空跑

写「切片不截断句子」这条测试时，我先用默认分隔符跑了一遍，
确认断言**确实能抓出** 1 处截断，才认为这条测试是有效的。
断言写得再漂亮，如果它在错误实现下也通过，那就等于没写。

---

## 三、演示准备

演示前确认这几件事：

- [ ] `.env` 里填了有效 key，`uv run python ingest.py` 跑通
- [ ] `uv run pytest -q` 全绿（21 项）
- [ ] `run_web.bat` 能起服务，浏览器正常打开
- [ ] 准备 3 个演示问题：一个文档内有答案的、一个需要多轮追问的、
      **一个知识库里查不到的**（用来展示拒答行为，这是本项目最值得看的一点）
- [ ] 本地演示 `docs/` 放新文档 → 点「重建索引」→ 立刻能问到，展示知识更新的闭环

---

## 附录：项目早期的原始描述（存档）

> 这段文字是本项目最早的一份项目描述草稿，内容已扩写并整理进 README。
> 此处原样存档以免丢失。原文末尾在「…重试」完整」处截断，未做补全。

```
技术栈：Python3.13、LangChain1.4、Chroma、BAAI/bge‑small‑zh‑v1.5、Streamlit、uv、Anthropic Messages API  项目简介 面向私有.txt/.md文档的检索增强生成 RAG 智能问答 Agent。用户自然语言提问，Agent 自主决策是否调用检索工具；检索召回不足时自动改写查询关键词重试；同时提供命令行、Web 两套交互界面  主要工作与亮点  Tool‑Calling Agent 编排 基于 LangChain1.x create_agent实现工具调用智能体，自定义知识库检索工具；借助 checkpointer 持久化维护多轮对话上下文，支持多轮追问；实现「模型自主判断检索时机‑检索不足自动改写关键词重试」完整
```
