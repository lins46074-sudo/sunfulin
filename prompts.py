"""提示词集中管理。

对应你那套轻量版 RAG 提示词。在 LangChain 1.x 的 create_agent 架构下，
其中两条由框架自动保证（见文件末尾说明），其余原样使用。
"""

# ---------------------------------------------------------------
# 1) Agent 系统提示词 + 5) 极简补充约束（末尾已合并）
# ---------------------------------------------------------------
SYSTEM_PROMPT = """你是一个简单的RAG问答智能体。你拥有知识库检索工具。

规则：
1. 用户问题需要用到私有文档内容 → 调用检索工具查询知识库；普通常识问题可以直接回答，不用检索。
2. 如果一次检索得到的内容不够回答问题，可以修改关键词再次调用检索工具。
3. 绝对不能编造文档内容、编造数据。知识库没有相关信息，直接说：【知识库没有找到相关内容】。
4. 回答时，所有来自检索文档的内容，标注来源编号，不要大段复制原文，用自己话总结。
5. 提问模糊缺少信息时，直接问用户补充，不要自己猜。
6. 工具调用阶段只输出工具调用，不要写最终答案。拿到检索结果之后，再生成回答。

回答尽量简洁易懂，分点说明。不要输出你的内部思考过程，只给用户最终答案。"""


# ---------------------------------------------------------------
# 3) 检索工具描述
# ---------------------------------------------------------------
# 说明：LangChain 里工具描述写在函数 docstring 上，
# 因此这段文字已落到 rag.py 的 search_knowledge_base 函数文档字符串中，
# 此处保留原文备查，修改工具描述请直接改 rag.py。
TOOL_DESCRIPTION = """工具名字：知识库检索
工具说明：用来查询我的私有文档资料。当用户问文档里的内容时调用这个工具。
入参：query，字符串，把用户问题改成适合搜索的关键词。
返回：相关文档片段+文档编号。
适用场景：查询上传的文档、文档里的知识点、文档中的数据。
不适用：日常常识、不需要看文档的问题。"""


# ---------------------------------------------------------------
# 2) 检索结果拼接提示词 —— 无需使用
# ---------------------------------------------------------------
# 在 LangChain 1.x 中，工具函数的返回值会被框架自动包装成 ToolMessage
# 交回给模型，不需要我们手工拼「下面是知识库检索出来的文档片段…」这段模板。
# 工具返回值本身已带文档编号（见 rag.py），对应规则 4 的来源标注要求。


# ---------------------------------------------------------------
# 4) 用户提示词模板 —— 无需使用
# ---------------------------------------------------------------
# create_agent 内部已实现「一步步解决用户问题」的循环，
# 用户输入直接作为消息传入即可。

# ---------------------------------------------------------------
# 6) 「工具调用阶段只输出工具调用」—— 由框架保证
# ---------------------------------------------------------------
# create_agent 内部就是一个 while(tool_calls) 循环：模型返回 tool_calls
# 时框架去执行工具，拿到结果后才让模型生成最终答复。
# 保留这条提示词无害，但不需要依赖它来约束行为。

WELCOME = """
================ 轻量版 RAG 问答智能体 ================
· 直接提问，会用知识库检索工具查你的私有文档
· 输入 /exit 退出
=======================================================
"""
